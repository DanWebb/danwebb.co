import styled from 'styled-components';

const Toolbelt = styled.div`
	display: flex;
	flex-wrap: wrap;
	align-items: center;
	margin-left: -7px;
	width: calc(100% - 60px);
`;

export default Toolbelt;
